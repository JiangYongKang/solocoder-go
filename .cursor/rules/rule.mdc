---
alwaysApply: true
---

项目目标
本项目用于 AI 编程模型的数据标注。通过设计具有挑战性的编程任务，测试模型在真实工程环境下的代码生成，需求迭代，错误修复，代码重构的能力。

核心概念
一个任务 Task 对应一次迭代，迭代可以是代码生成，需求迭代，错误修复，代码重构中的一种。例如：注册登录功能开发，个人主页需求迭代，设备管理的错误修复，用户模型的代码重构等。每个任务下包含多个对话会话 Session，这种一对多的层级结构反映了真实开发中 "一个需求模块经过多轮迭代" 的过程。

核心流程
1. 每个会话交付一份 prompt-N.txt 给被测模型（N 为会话序号，从 1 开始），被测模型按提示词完成代码开发。
2. 被测模型完成后，会将其思考过程生成到 tasks/taskXXX/session-cot-N.md。
3. 标注人员用 Git diff 查看代码变更，用 go test -v 验证模型产物，然后结合思考过程编写 assessment-N.txt，识别产物错误和过程问题。
4. 标注人员将 assessment-N.txt 中的标注以自然语言提交到标注平台。

任务生命周期
每个任务的生命周期按 Session 顺序推进，同一任务下的所有 Session 复用同一个代码库和 docs 目录下的需求文档。
1. 功能开发会话：给模型一个基础项目骨架和需求文档，让它在空白代码上从零构建新功能模块。考察架构设计、技术选型、API 设计等。
2. 缺陷修复会话：基于上一轮会话的标注结果（assessment-N.txt），给模型一段已有代码，让它修复其中的 BUG。任务是纯修复，不做新功能扩展。考察调试能力、问题定位、对修改范围的控制。

提示词编写规范
1. 提示词是给模型看的，要求模型做什么，但不要告诉它怎么做。每一条只讲"现在是什么问题"或"需要什么功能"，不写"应该用什么结构体、调什么方法"。
2. 提示词必须是自然语言描述，简洁清晰。每一条要求尽量一两句话描述完成，不需要描述很多细节。太多的细节反而会限制被测试模型的灵活性。
3. 不同任务要有明确的包隔离。每个任务的所有代码应集中在语义对应的单一包下，不能一个任务的代码散落在多个无关的包中。任务设计者会确保每个任务的功能足够独立，使其天然适合收敛在一个包内。

功能开发提示词示例
```
在当前项目中实现一个 HTTP 网关域功能模块，使用内存数据结构模拟上游服务。
1. 实现路由转发：支持精确匹配与通配符匹配两种路由规则，将请求按规则转发到对应的上游处理函数，未匹配的请求返回 404。
2. 实现鉴权中间件：支持 Bearer Token 校验，Token 无效或缺失时返回 401，鉴权通过后将用户信息注入请求上下文传递给下游。
3. 实现熔断降级：对每个上游服务维护一个滑动窗口的失败计数器，连续失败超过阈值时熔断器打开，后续请求直接返回降级响应而不再调用上游，熔断器打开一段时间后进入半开状态允许少量探测请求。
4. 代码实现和单测需要放在 internal/gateway/ 包下，然后为该模块编写完整的单元测试，覆盖正常流程、边界条件和异常分支。
5. 运行 go test ./internal/gateway/ -v，需要让所有测试通过。
6. 在 docs/gateway.md 中编写模块需求文档，说明模块功能、核心结构体的职责、熔断器状态流转图（可用文本描述）以及使用示例。
```

缺陷修复提示词示例
```
你实现的内存 KV 存储模块存在一些问题，需要修复：
1. 当前实现存在并发安全问题，多个 goroutine 同时读写存储时可能出现数据不一致。需要使用分段锁或 sync.RWMutex 保护共享状态。
2. 布隆过滤器的误判率计算有误，当存储条目超过预期容量时误判率从 1% 飙升到 15%，导致大量不必要的磁盘读取。
3. 快照导出逻辑在导出期间没有冻结写操作，导出的快照可能包含部分写入的数据，导致快照恢复后数据不一致。
4. 修复后需要为该模块补充并发读写场景的单元测试，验证多 goroutine 下的数据一致性和快照导出的一致性。
5. 运行 go test ./internal/kvstore/ -v，需要让所有测试通过。
6. 在 docs/kvstore.md 中更新模块需求文档，说明修复后的并发保证策略和布隆过滤器容量配置约定。
```

标注规范
1. 每个会话的标注放在 tasks/taskXXX/assessment-N.txt 中（N 为会话序号）。
2. 每一条标注内容必须在 150 ~ 250 字之间。
3. 标注文件为纯文本格式，不使用任何 Markdown 符号，只使用最基础的逗号，句号，双引号，错误信息使用反引号作为代码块。
4. 标注内容必须忽略细节，比如第 xxx 行，比如第 xxx 步，只需要描述哪个文件中或哪个方法中即可。
5. 段落结构分为两段：
    - 产物不满意：列出代码实现中的具体缺陷。
    - 过程不满意：从模型思考过程中分析模型行为和决策路径的问题。
6. 只关注当前任务所在包的代码。例如被测模型在 A 包下开发了功能代码、单元测试、文档。那么审查代码，执行单测，分析思考轨迹都只针对 A 包。涉及到 B 包的问题，不应纳入当前任务的标注内容。

标注要素：一条合格的不满意原因尽量具备以下五个要素，每个要素用一句话说清楚，采用自然口语化的中文表达，避免过度专业术语：
1. 触发节点（When）：在什么情况下触发的问题？
2. 实际行为（What）：触发的问题是什么？
3. 业务影响（Impact）：这个错误导致了什么后果？
4. 根本原因（Root Cause）：造成这个问题的原因是什么？
5. 解决方案（Solution）：解决这个问题的正确做法是什么？

产物不满意原因的书写准则
1. 必须定位到具体文件、函数或组件：指出问题出在哪个文件的哪个函数中，不能笼统地说"某功能有问题"。
2. 必须有具体证据支撑：引用报错信息原文、出错的函数名或文件名、提示词中未满足的具体需求编号。禁止使用"效果不好""不行""一般"等笼统形容词作为理由。
3. 接口调用错误时，根据返回的状态码 400 还是 500，落实到具体的前端请求逻辑或后端接口问题上，不能只写"接口报错"。
4. 页面空白、404 等问题，落实到具体的渲染逻辑或路由配置上，不能只写"页面打不开"。
5. 项目编译不过或启动不起来，要解释具体的编译错误、依赖冲突或语法问题，不能只写"项目跑不起来"。
6. 业务逻辑问题要具体指出哪个业务场景未完成，并给出反例，不能只写"逻辑不对"。
7. 美观度、用户体验、交互流畅性等主观感受可以写，但不能作为主要评判标准，重点仍要落在模型代码实现的客观问题上。
8. 如果产物问题可能是由过程问题导致的，应采用闭环表述：先指明在第几步或哪个环节模型做了什么具体行为，再说明这导致产物在哪个文件或函数中出现了什么问题，问题的客观证据（报错或者没满足提示词中的哪一条需求）是什么？原因和解决方案是什么？

过程不满意原因的书写准则
1. 必须定位到具体环节和步骤：指明问题发生在哪个步骤、哪个工具调用、哪次文件改动。不能只笼统地说"过程有问题"。
2. 必须有具体的行为证据支撑：从模型思考过程中引用具体的行为模式和行为序列作为证据，不能空洞地说"模型思考不清楚"。
3. 过程不满意原因的表述模板：模型在第几步或哪个环节，做了什么具体行为，导致什么后果，应该怎么做

从以下角度检查过程质量
1. 目标理解：模型是否充分理解了提示词要求。例如要求修 BUG 却没修，或修复范围与任务无关，说明对目标把握偏差。
2. 推理效率：是否在同一代码块反复修改、反复推翻之前的逻辑、反复查看相同文件的相同区域。分批查看同一文件的不同行是合理的，但无意义的重复读取是低效信号。
3. 任务规划：TodoList 是否合理、是否按规划执行、是否所有子任务都完成。注意不能在刚收到任务时就直接创建 TodoList，应先分析问题再规划。
4. 工具使用：是否采用正确的方式查找和操作文件，先 Grep 定位相关文件路径，再用路径读文件，整理出所有关联文件后再修改，而非零散地读一个改一个。已打开且在 IDE 中停留的文件可视为已知。
5. 边界意识：是否执行了危险命令，是否做了提示词明确禁止的事项，缺少边界意识和合规校验是过程硬伤。
6. 幻觉与虚假完成：是否在总结中描述了未实际执行的操作，是否在未运行测试或未启动项目的情况下声称完成。
7. 显式错误：回复使用英文、工具调用命令出现在思考或回复正文中等，属于基本的输出规范问题。
8. 试错式调试：遇到编译或运行错误后直接换方案重写，再失败再换方案，而不是先分析错误信息、缩小问题范围、做最小修正。同一类错误出现两轮以上未修正是强信号。
9. 生产代码向测试妥协：写测试时遇到 mock 困难，应该调整测试方案而非改变生产代码的架构。

标注示例一
```
产物不满意：
1. 在编写状态机时 Transition 方法只实现了待支付到已支付的正常流转分支，退款相关的 REFUND_REQUESTED 和 REFUNDED 状态及对应的转移路径完全缺失，fulfillment 调用退款时直接返回 `"invalid transition"` 错误导致售后场景无法处理。原因是没先列出完整状态转移矩阵就动手编码，应先梳理所有状态和合法转移再实现。
2. promotion.go 的 ApplyPromotions 函数对满减和折扣两类促销采用了简单累加策略，两个互斥促销同时命中时金额被双重优惠扣减了 15%，与提示词要求互斥促销不能同时生效矛盾。原因是没先设计互斥规则表对促销做分组处理，应在互斥组内只选取优惠力度最大的方案而非简单累加全部活动的折扣。
3. fulfillment.go 的 ShipLineItem 方法在部分发货时将已发数量累加到订单级别而非行项目级别，第二次发货后 TestPartialFulfillment 测试失败报 `ShipLineItem: expected fulfilled_qty=2, got 3`，已履约数量超出预期。原因是混淆了订单维度和行项目维度的履约计数器，应在每个 lineItem 上维护独立的 FulfilledQty 字段。
过程不满意：
1. 模型在刚收到提示词后未经分析就直接用 todo_write 创建了 6 项子任务计划，之后才开始阅读 docs 目录下的需求文档和现有代码结构，导致计划的粒度划分与实际模块边界不匹配，后续多次在 state_machine 和 fulfillment 之间反复调整架构。正确做法是先分析需求文档和代码骨架，理清模块职责边界后再制定执行计划。
2. 写完 promotion.go 后模型在回复中声称促销模块已完成但并未运行 `go test ./internal/order/` 验证代码正确性，直到后续编写测试时才通过失败用例反推出互斥校验逻辑完全缺失。这属于典型的幻觉与虚假完成，模型把代码写完了等同于功能实现了而跳过了测试验证环节。正确做法是每完成一个模块就立即运行对应单元测试以失败结果驱动下一轮修复。
```

标注示例二
```
产物不满意：
1. 提示词要求修复惰性过期回收，但模型只在 lru_cache.go 的 Get 方法里添加了 TTL 检查，Set 和 Contains 这两个同样会访问缓存数据的方法完全没有处理过期逻辑。TestExpiredEntryInSet 和 TestExpiredEntryInContains 均失败报 `expected nil for expired key, got cached value`。原因是只修补了最显眼的 Get 入口而没系统性扫描所有访问点，应先找出全部读取缓存的方法再逐一添加过期驱逐。
2. lru_cache.go 的 evictIfNeeded 方法把互斥锁的 Lock/Unlock 放在 for 循环外层，当缓存容量较大时驱逐操作持锁超过 3 秒，导致 TestConcurrentReadDuringEviction 超时失败。根因是锁粒度过粗把不需要串行化的清理操作也纳入了临界区，应改为在循环内部逐条加锁或用 sync.RWMutex 分离读和写路径。
3. lru_cache.go 中 isExpired 方法使用 time.Now() 获取当前时间，而 cleanupExpired 使用 time.Since() 计算差值，两个函数对同一时刻的判定逻辑不一致，导致同一条目在 Get 返回有效数据后 0.1 秒内就被 cleanup 当作过期条目误回收。根因是模块内时间判定源不统一，应全模块统一使用 time.Since() 与 TTL 做比较。
过程不满意：
1. 执行 go test 发现测试失败后模型没有先阅读失败用例的输出信息就直接修改 lru_cache.go 中的三处代码，再次编译报 `syntax error: unexpected newline`，随后又把刚才的修改撤了回去。这是典型试错式调试，同一处代码两轮修改仍未修正，正确做法是先读测试输出定位具体错误再只对出错处做最小范围的修改。
2. 在编写并发测试时模型在 test_concurrency.go 中定义了 mockCache 结构体来替换真实的 Cache 实例以规避真实并发场景的复杂度，但生产代码本身依赖 sync.Mutex 的锁语义，mock 后测试覆盖的是 mock 对象而非真实的并发控制行为。这是生产代码向测试妥协，正确做法是用 sync.WaitGroup 和 channel 同步 goroutine 执行时机而非用 mock 绕过并发难点。
```

标注示例三
```
产物不满意：
1. dlq.go 的 MoveToDLQ 方法在消息超过最大重试次数后将其转移到死信队列，但转移时丢失了原消息的首次入队时间和已投递次数。提示词要求支持死信消息的二次重试，缺失这些溯源信息根本无法判断一条死信是否还值得重新投递。原因是只关注转移动作本身而忽略了重试决策所需的上下文，应在死信消息体中保留 OriginalEnqueueTime 和 DeliveryCount。
2. ReceiveMessage 方法中的可见性超时机制只在消费者显式调用 Ack 时才停止计时器，如果消费者进程异常崩溃没有执行 Ack 该消息就会永远对其它消费者不可见。TestVisibilityTimeoutOnCrash 测试失败报 `expected message to be visible again after timeout`。原因是未实现超时未确认消息的被动重投，应增加后台定时扫描 goroutine 将超时未 Ack 的消息重新放回可见队列。
3. Publish 方法的去重校验只比较消息体的内容而忽略了 MessageID 字段，导致两条 Body 完全相同但 ID 不同的独立消息被错误判定为重复而丢弃。TestMessageDeduplication 失败报 `Publish: expected 2 messages, got 1`。根因是把消息内容去重和投递幂等性两个概念混淆了，应以 MessageID 作为去重的唯一键而不是比较消息体内容。
过程不满意：
1. 模型在创建 internal/queue/queue_test.go 后直接运行 go test，此时抛出 `package solocoder-go/internal/queue is not in std` 找不到包导入路径。原因是缺少对项目基础设施的前置分析，没有先阅读 go.mod 理解 Go module 的包路径和 internal 目录的访问规则。合理做法是在开始编码前先查看项目骨架，了解 module 配置、包目录结构和导入路径规则。
2. 看到 5 个测试用例失败后模型没有逐个阅读失败信息就直接将 dlq.go 整个文件重写了一遍，重写后又引入新的缺陷，其中对去重逻辑的处理经历了添加 MessageID 校验、又将其删除、又重新加回来的三轮相反方向修改。这是三轮不收敛的试错循环，正确做法是依次分析每个失败用例指向的具体代码再逐一做最小修正。
```

目录结构

```
solocoder-go/
  internal/                        业务代码（按域分包，一个 task 一个包）
    gateway/                       HTTP 网关域（TASK_001）
      middleware.go
      router.go
      circuitbreaker.go
      gateway_test.go
    shardkv/                       内存分片存储域（TASK_002）
      ...
    orchestrator/                  并发任务编排器（TASK_003）
      ...
    kvstore/                       内存 KV 存储域（TASK_004）
      ...
    logrotator/                    日志文件轮转器（TASK_005）
      ...
    delaysched/                    延迟任务调度器（TASK_006）
      ...
    connpool/                      连接池管理器（TASK_007）
      ...
    secenv/                        安全信封模块（TASK_008）
      ...
    eventsrc/                      事件溯源引擎（TASK_009）
      ...
    jobqueue/                      内存任务队列（TASK_010）
      ...
  docs/                            需求文档
    gateway.md                     HTTP 网关域详细需求
    shardkv.md                     内存分片存储域详细需求
    orchestrator.md                并发任务编排器详细需求
    kvstore.md                     内存 KV 存储域详细需求
    logrotator.md                  日志文件轮转器详细需求
    delaysched.md                  延迟任务调度器详细需求
    connpool.md                    连接池管理器详细需求
    secenv.md                      安全信封模块详细需求
    eventsrc.md                    事件溯源引擎详细需求
    jobqueue.md                    内存任务队列详细需求
  tasks/                           标注工作区
    feature.md                     全部 10 个任务的需求概览
    task001/                       任务：HTTP 网关域
      prompt-1.txt                 第 1 轮提示词（功能开发）
      session-cot-1.md             第 1 轮模型思考过程
      assessment-1.txt             第 1 轮标注分析
      prompt-2.txt                 第 2 轮提示词（缺陷修复，选填）
      session-cot-2.md             第 2 轮模型思考过程（选填）
      assessment-2.txt             第 2 轮标注分析（选填）
      prompt-3.txt                 第 3 轮提示词（缺陷修复，选填）
      session-cot-3.md             第 3 轮模型思考过程（选填）
      assessment-3.txt             第 3 轮标注分析（选填）
    task002/                       任务：内存分片存储域
      prompt-1.txt
      session-cot-1.md
      assessment-1.txt
      ...
    task003/                       任务：并发任务编排器
      ...
    task004/                       任务：内存 KV 存储域
      ...
    task005/                       任务：日志文件轮转器
      ...
    task006/                       任务：延迟任务调度器
      ...
    task007/                       任务：连接池管理器
      ...
    task008/                       任务：安全信封模块
      ...
    task009/                       任务：事件溯源引擎
      ...
    task010/                       任务：内存任务队列
      ...
  main.go                          项目入口
  go.mod                           依赖和模块配置
  go.sum                           依赖校验
  README.md
  .gitignore
```
