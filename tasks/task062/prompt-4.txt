你修复的资源配额管理器文档仍有两个问题未解决，需要修复：
1. docs/quotamgr.md 第 8.2 节修复后的链式箭头"全局写锁 → 租户级锁 → 全局读锁"没有标注各步锁的释放节点。实际实现中第一步通过 getOrCreateTenantUsage 获取全局写锁后在该函数内部立即释放，与后续租户级锁从未同时存在，但链式写法会让读者误读为三把锁嵌套同时持有的模式。需要在链式中明确标注释放时机。
2. 三条锁规则中第一条以 getOrCreateTenantUsage 和 getTenantUsageIfExists 作为示例函数，但这两个函数实际上只获取了全局锁而从未在函数体内获取租户级锁，完整的锁模式需要调用方配合才能完成。维护者根据文档追踪这些辅助函数时看不到租户级锁的操作会怀疑示例写错了。需要为示例函数补充调用关系的说明以使示例与规则描述的行为完全吻合。
3. 修复后运行 go test ./internal/quotamgr/ -v，需要让所有测试通过。
