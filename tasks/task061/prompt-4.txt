你修复的超时传播器模块在上轮引入了新的问题，需要修复：
1. Execute 方法的错误处理中追加了 isRootDeadline 双重检查，而 WithTimeout 修改后 stageCtx.Err() 已稳定返回 DeadlineExceeded。当预算阶段 goroutine 返回业务错误且根 context 恰巧过期时，isRootDeadline 为真会错误地将业务失败分类为超时，err 被覆盖为 DeadlineExceeded，调用方无法通过 errors.Is 识别真实错误类型。这个双重检查是完全多余的，应删除它仅保留 isDeadline 判断。
2. 修复后运行 go test ./internal/timeoutprop/ -v，需要让所有测试通过。
