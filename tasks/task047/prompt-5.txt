你实现的内容安全策略引擎模块将 SVG data URI 修复得过于激进，需要调整：
1. processAttributes 方法将 `image/svg+xml` 完全排除在白名单外，导致不含任何脚本和事件处理器的合法 SVG 图标也被拦截。提示词要求对 SVG 做脚本内容检测或仅放行不含事件处理器和 script 标签的版本，而非全量封锁。应恢复对 image/svg+xml 的支持，但在放行前检查 SVG 内容中是否包含 `<script` 标签或 `onload`、`onclick`、`onmouseover` 等事件处理器属性，含危险内容的 SVG 才拦截，安全 SVG 正常放行。
2. 运行 go test ./internal/contentsec/ -v，需要让所有测试通过。
