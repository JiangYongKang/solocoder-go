你实现的可插拔序列化框架存在一些问题，需要修复：
1. protobuf.go 的 getPBFieldInfo 函数中 Protobuf 标签解析完全失效，用户通过 `serialize:"name,protobuf:N"` 声明的字段号被静默忽略，字段号退化到按结构体字段顺序自动分配，需要修复标签解析逻辑使其正确提取用户声明的字段号。
2. msgpack.go 的 decodeBytes 方法中零拷贝优化路径 `zeroCopyBytes(string(b))` 存在隐式内存拷贝，Go 的 string(b) 转换会分配新内存并拷贝数据，导致零拷贝优化名存实亡，需要在字节数组字段的反序列化路径中正确复用底层缓冲区。
3. serialize_test.go 的 TestMsgPackEdgeCases 中 12 个子测试的函数体完全为空，没有执行任何序列化或反序列化操作，需要在每个子测试中补充往返编解码逻辑和断言，确保边界数据类型被正确覆盖。
4. 修复后运行 go test ./internal/serialize/ -v，需要让所有测试通过。
