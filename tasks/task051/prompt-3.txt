你实现的可插拔序列化框架在第二轮修复后仍存在残留问题，需要继续修复：
1. protobuf.go 的 getPBFieldInfo 函数中未声明 protobuf 标签字段的回退字段号从原来的 +1 变成了 +2，这会导致已编码的历史数据无法兼容。需要恢复旧版本的回退编号规则，或提供迁移兼容策略确保历史数据仍能正确反序列化。
2. protobuf.go 的 getPBFieldInfo 函数按逗号分割标签后直接把 parts[0] 当作字段名，没有检查它是否就是 "protobuf" 关键字本身。当标签格式为 `serialize:"protobuf:N"`（省略字段名只声明字段号）时，字段号声明被静默跳过。需要让两种标签格式都能被正确解析。
3. 修复后运行 go test ./internal/serialize/ -v，需要让所有测试通过。
