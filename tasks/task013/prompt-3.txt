你实现的 EventBus 事件总线模块在第2轮修复中代码实现正确但文档未同步更新，需要修复：
1. docs/eventbus.md 中第 3.1 节仍保留"为每个匹配的订阅者启动一个独立的 goroutine"和"异步模式下忽略订阅者返回的 error（包括 ErrInterrupt）"的旧版描述，与第2轮修复后单 goroutine 顺序执行且支持中断的实际实现矛盾。
2. docs/eventbus.md 中第 3.2 节仍写着"中断机制仅在同步分发模式下生效"，与修复后异步模式同样支持中断的策略矛盾。
3. docs/eventbus.md 中第 6 节错误处理表中 ErrInterrupt 标注为"仅同步模式"，第 8 节模块对比表同样标为"支持（同步模式）"，需要更新为反映异步模式也支持中断的实际情况。
4. 提示词第 5 条要求"说明异步分发模式下优先级和中断机制的具体实现策略"，但文档的核心算法章节完全没有体现这一要求。
5. 修复完成后运行 go test ./internal/eventbus/ -v，需要让所有测试通过。
6. 在 docs/eventbus.md 中更新模块需求文档，确保所有章节描述与代码实现一致。
