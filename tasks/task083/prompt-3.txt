你实现的时序异常检测器模块在配置变更时仍存在数据完整性问题，需要修复：
1. UpdateConfig 从季节性模式切换到非季节性模式时，已积累的槽位数据被重复计入全局基线，导致基线数据量虚增一倍。
2. UpdateConfig 在调整 PeriodLength 参数时，旧槽位的数据被复制到了代表不同周期相位的新槽位上，不同相位的数据不应共享。
3. 修复后运行 go test ./internal/tsanomaly/ -v，需要让所有测试通过。
