你实现的舱壁隔离模块仍存在残留问题，需要继续修复：
1. bulkhead.go 的 SemaphoreFullError 结构体在新增 WorkerActive 字段后，ActiveCount（值为 workerActive + semHolders）和 SemHolders 两个字段之间存在语义重叠：ActiveCount 已经包含了 SemHolders，SemHolders 是 ActiveCount 的一个子集。调用方在解析错误信息时可能误用 ActiveCount - SemHolders 来计算 WorkerActive 而非直接使用新增的 WorkerActive 字段，存在因冗余字段引发误用的风险。需要在 SemaphoreFullError 结构体定义处添加清晰的注释，说明每个字段的含义、计算来源以及它们之间的包含关系。
2. 修复后运行 go test ./internal/bulkhead/ -v，需要让所有测试通过。
