你实现的 JWT 令牌管理器模块存在一些问题，需要修复：
1. 当前 docs/jwtmgr.md 文档内容严重不完整，仅包含模块概述和核心功能表格两节，从第 3 节"核心结构体与职责"开始往后所有章节均为空白，缺失结构体职责说明、令牌签发与校验流程、黑名单吊销机制、令牌续期机制、刷新令牌轮换机制以及使用示例。
2. MemoryBlacklist 的 Blacklist 接口中，Add 方法对空 tokenID 返回了错误，而 Contains 方法对空 tokenID 返回了 false, nil，两个方法对同一非法入参的行为约定不一致，调用方无法区分"黑名单中没有该 token"与"传入空字符串"两种语义。
3. MemoryBlacklist 在 Close 方法执行后再调用 Add，检测到 closed 状态后直接返回 nil 错误且不做任何存储，调用方收到 nil 返回值会认为黑名单操作已成功，实际 token 被静默丢弃，构成隐蔽的数据丢失风险。
4. 修复后需要为上述问题补充对应的单元测试，验证错误处理一致性、Close 后 Add 的拒绝语义以及文档中声明的所有流程的正确性。
5. 运行 go test ./internal/jwtmgr/ -v，需要让所有测试通过。
6. 补全 docs/jwtmgr.md 文档中所有缺失章节的内容。
